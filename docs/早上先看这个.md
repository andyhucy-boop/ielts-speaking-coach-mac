# 早上先看这个

夜间无人值守的进展与交接。**只有这一份需要你先读，其余是给实现者看的。**

最后更新：2026-08-08（成品终审之后）

---

## 一、跑到哪了

| 阶段 | 状态 |
|---|---|
| Phase 0–2（探路、地基、驱动 + 命令行）| ✅ 完成 |
| Phase 3（界面骨架、三个页面、打包签名、PDF 导入、点按钮真能开练）| ✅ 完成 |
| Phase 4（逐字稿采集、训练记录页、单条删除、复盘补救界面）| ✅ 完成 |
| Phase 5（录音与回听）| ✅ 完成 |
| Phase 6（复训中心）| ✅ 完成 |
| Phase 7（问题档案、词汇本、统计趋势）| ✅ 完成 |
| Phase 8（学习计划页 + 四条路线）| ✅ 完成 |
| Phase 9（MCP server + Codex 插件）| ✅ 完成 |
| Phase 10（打包分发 + 深色模式 + 设置合并 + 两个占位页）| ✅ 完成 |
| **成品复审（13 条必修缺陷）** | ✅ 全部修完 |
| **成品终审（独立复核 + 复现验证）** | ✅ 完成 |
| **你的真机验收** | ⏳ **一件都没做——见第三节** |

**测试 1857 条全绿，20.2 秒。**（复审前是 1744 条；修 13 条补进来 111 条，终审又补 2 条。）

---

## 二、一句话结论

**可以装上开始用了，而且可以开始把它当成你的练习记录本——但先花二十分钟跑一遍第三节。**

完整报告：`docs/superpowers/FINAL-REVIEW.md`。

十个阶段做完之后，一轮成品复审实测出 13 条你在正常使用中真会撞上的缺陷，**现在全部修完**。
其中四条最狠的，终审又把修复逐条拆掉、看着原缺陷复现、再装回去——所以下面这四句话是有证据的，
不是「读代码觉得没问题」：

- **按「取消」之后，它不会再操作你的 ChatGPT、不会再动你的剪贴板，麦克风也不会被重新打开。**
  （拆掉守卫实测：取消之后麦克风真的被重开、剪贴板真的被清空、明确放弃的那一场真的带着完整复盘进了训练记录。）
- **每份复盘里那三块此前永远看不到的内容（整体总结、口语习惯、逐题逻辑反馈）现在都上屏了。**
  「回答前有较长停顿」「没有先直接回应问题」这类最该被你看见的反馈，此前一条都不显示。
- **连点两下「我练完了」只跑一条收尾链路**，那段一千多字的复盘请求不会被打进 ChatGPT 两遍。
- **「删除这一场」遇到被改坏的路径会整趟拒绝、一个字节都不动**，还会点名告诉你去 `state.json` 的哪一行改什么。
  （拆掉那道闸实测：`state.json` 真的被删、录音目录真的被递归删光、数据目录**外面**的文件真的被删。）

**你的现有数据不需要迁移，也不需要你做任何事**，三件事都是打开 App 就自动生效：

1. 以前报「训练数据文件已损坏」打不开的 `state.json`，现在直接就能打开
   （如果你已经照旧提示把它改名备份了，把名字改回 `state.json` 即可，历史一条没丢）；
2. 换季重新导入题库时被抹掉的「已练」标记，会在下一次读盘那一刻自动算回来；
3. 删除一场训练记录现在拦得住被改坏的路径。

---

## 三、需要你亲自做的（程序绕不过去，一件都还没做）

**这是现在唯一挡在「能不能日常用」前面的东西。** 十个阶段的实施计划里每个阶段都留了一步
「真机验收（人工，不可由子代理代劳）」，`docs/phase*-acceptance-checklist.md` 里清单写得很详细，
**但一份填好的结果都没有**。全部 1857 条测试证明的是「逻辑对」，不是「能用」。

### 1. 签名稳定性（5 分钟，先验这个）

```bash
cd ~/Projects/ielts-speaking-coach-mac && ./scripts/build-app.sh && open ".build/IELTS Speaking Coach.app"
```

**系统设置 › 隐私与安全性 › 辅助功能 → 把 `IELTS Speaking Coach` 加进去并勾选 → 回 App 点「重新检查」。**

然后**再跑一次 `build-app.sh`，不重新授权直接打开**——如果又要求授权，打包方案要重做，停下来告诉我。

终审已经替你跑掉了一半：脚本连跑两次都通过，「指定要求」与 `packaging/expected-designated-requirement.txt`
逐字一致。**剩下那一半（真授权一次再打包）只有你能做。**

### 2. 麦克风真的能录（10 分钟）

签名里写了权限不等于系统真的放行。打开「保存我的回答录音」时点「允许」，真录一场，回听。
然后：**戴耳机说 30 秒 → 拔掉说 30 秒 → 插回说 30 秒**，确认只有一条录音、三段都在、总时长约 90 秒。

### 3. 走一遍完整练习，把终端关掉（30 分钟）

- 导入你真实的 PDF 题库 → 数一下从双击图标到 ChatGPT 开口一共点了几次（目标 ≤ 3）
- 盯着那 9 秒的启动等待，看界面有没有一直在告诉你它在干什么
- 练完点「我练完了」，看错题本 / 词汇本 / 重训目标三处数字有没有从 0 变正
- **顺手验复盘页**：应该能看到「整体总结」「口语习惯」「逐题逻辑反馈」三块
  （这是第 2 条缺陷的修复，也是唯一能证明「屏幕上真的有」的办法——扫源码证明不了渲染）
- **顺手验红线**：复盘里不该出现任何雅思分数、评级、水平判断。
  这条只能靠提示词拦（那段字是 ChatGPT 写的），界面侧拦不住，所以要你亲眼看一次

### 4. 逐条核对逐字稿（这一步最重要，只有你能做）

打开训练记录，对着 ChatGPT 窗口逐条核：考官问过的每一个问题都在吗？顺序对吗？分得清谁说的吗？

**说话人判别在语音会话里成不成立，至今没有任何人验证过**，这是这个项目最大的未知。
设计上「判不出就记 unknown，绝不猜」——最坏情况只是分不清谁说的，内容和顺序照样完整。
`docs/phase4-acceptance-checklist.md` 里有「什么现象改哪个文件」的对照表。

### 5. 剩下这些（做完上面四件再说）

真删一条训练记录去数据目录核对文件、练到一半点「放弃这一场」看那张交代卡片、
练到一半关掉 ChatGPT 看能不能不开终端救回来、深色模式 + 减弱动态效果 + 最大字号、
把 .app 拷给另一个人看 Gatekeeper 怎么拦。逐条见 `FINAL-REVIEW.md` 第 6 节。

### 6. 最后一条，也是唯一真正的判据

**你会连着用它练一周吗？** 如果做完之后你还是宁愿直接打开 ChatGPT 自己敲提示词，
那这个工具就没做成——不管功能表打了多少勾。**「哪里让我不想用」这类信息只有你有。**

---

## 四、我替你做的决策，需要你复核

全部在 `docs/superpowers/DECISIONS-2026-08-06.md`，每条都标了改主意的成本。
复审这一轮又多了四处「实现者刻意没照报告建议做」的取舍，逐条写在 `FINAL-REVIEW.md` 对应条目里，
终审复核后都同意，摘要：

- **删除遇到坏路径时「整趟拒绝、连记录也不删」**，而不是「跳过并报出来」——
  那条记录是你找到坏路径的唯一线索，先删掉就等于让你修无可修。
- **放弃练习时不自动删掉那段录音**，只把路径报给你并指向「打开录音文件夹」——
  那几分钟是你真的说过的话，一次点击就静默删掉比留一个孤儿文件更难挽回。
- **`PracticeSession.id` 保持必需，不做容错**——没有 id 的记录在界面上无法寻址，
  兜一个空串会让两条这样的记录在列表里撞成同一行，你删掉一条会看到另一条跟着消失。
- **「半自动模式」没有做出来，改成把引导文案说成实话**——那是一整个新功能面，
  而做出来之后你仍要手动跑完整条流程；今天真正伤人的是「照着文案做完全部动作仍然一场都练不成」。

---

## 五、还没解决的一件事，我不打算继续投人

**扫源码证明不了渲染。** 把一段界面代码的声明和调用一起删掉，测试照样全绿。
这是「扫源码」这个手段的固有边界，不是漏了哪条断言。真要堵死得引入渲染层测试框架，
而本项目零第三方依赖是硬约束。

所以 `FINAL-REVIEW.md` 里所有界面结论严格讲都只是「代码里写了」，不是「屏幕上有」——
**第三节第 3 条就是干这个的**。

---

## 六、剩下的问题清单

`docs/superpowers/OPEN-ISSUES.md`（终审那一轮记在「负一节」）。每一条都是实测证明过的，不是猜测。
今天还在的，按后果从重到轻：

- 词汇导出的最后一跳（文本 → 文件字节）零测试，界面那句「已经存到 xxx.txt」跟磁盘上落了什么字节无关；
- 诊断的十个档位里有三个（录音、题库导入、生成计划）没有任何地方会写进去；
- 练习进行中界面每 2.5 秒顿一下（逐字稿采样在主线程遍历整棵界面树，不丢任何数据）；
- 还有 5 处文案指着不存在的按钮或把原因说反了；
- 取复盘时会清空并改写你的系统剪贴板，顺利路径上仍然没有一句话告诉你。
