import Foundation

// 占位入口：coach target 的完整 CLI 会在 Phase 2 后续任务中实现。
// 这个文件的存在是为了让 SPM 能找到 target 的源码目录——
// 已声明但没有源码目录的 target 会导致 SPM 回退去扫包根，报 overlapping sources。
print("coach — IELTS Speaking Coach CLI（占位，尚未实现）")
